// Colors
primary = #3f51b5
primary-light = #c5cae9
primary-dark = #1a237e

accent = #e91e63
accent-light = #f8bbd0
accent-dark = #ad1457

// Secondary text / Icons
grey = rgba(black, .54)
// Disabled / Hint text
grey-light = rgba(black, .26)
// Divider
grey-lighter = rgba(black, .12)


// Svg images as base 64
images = {
    thing: 'stuff'
}

getImage(image)
    url('data:image/svg+xml;base64,' + images[image])

// MQs
small = '(max-width: 800px)'
